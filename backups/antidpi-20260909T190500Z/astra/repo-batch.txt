Статус: **ядро + драйвер + живой прогон + продакшн-деплой**. Дата среза: 2026-09-01. Код: `internal/chaossync/ks.go` + `cmd/ks-vpn`. Заменяет CDT в роли data-plane (CDT снят с роли; код и документ CDT оставлены как лабораторная справка — идеи CST и ротации переиспользованы здесь). В carrier-режиме решётка — носитель, идущий по проводу (медленно: физика синхронизации). В KS решётка — **локальный генератор keystream на обеих сторонах**; по проводу идёт только `nonce‖ct`. Ключ никогда не передаётся.   и `EpochInit` эпохи — переиспользованы из `map.go`/`schedule.go` без изменений;   гейт Э1 и золотой хэш `29f2315f…` покрывают этот путь.   ChaCha20-Poly1305 (дефолт; AEGIS-128L заявлен опцией — пока честный stub,   в x/crypto проверенной реализации нет).   ничего не раскрывает.   CST-идея: выводится локально, на проводе не появляется.   Потерянная датаграмма = потерянный IP-пакет; следующая ра
---
Статус: **Э0–Э6 зелёные**. Дата среза: 2026-08-30. Код: `internal/chaossync` + `cmd/chaossync-server` + `cmd/chaossync-client` + `cmd/chaossync-selftest` + `tools/chaossync-lab`. Сеть — не поток дискретных символов, а **синхронизация двух хаотических осцилляторов** (Windows-клиент ↔ Linux RU-нода). По проводу идёт только квантованный скаляр `y_wire` — шумоподобный. Информация кодируется небольшими управляемыми возмущениями и считывается из динамики ошибки синхронизации `e = x_rx − x_tx`, а не из значений сигнала. Атаки Short 1994 / Pérez–Cerdeira 1995 (return-map / delay-embedding реконструкция) давятся конструкцией: векторное поле `f_k` **нестационарно**, мутирует по ключевому расписанию с периодом `T` — быстрее, чем цензор собирает окно реконструкции. Участников ровно два. Релеев нет. Этическая граница: только собственная динамика осцилляторов; **никакого** отравления измерителей цензо
---
Два экспериментальных транспорта Chameleon, интегрированные в   (`GenerateRandomIP`, LCG Numerical Recipes с отбраковкой зарезервированных   диапазонов RFC 6890 без смещения распределения). Статический IP-бан   неприменим: единого server IP нет.   L7 payload: Seq    = chunk[0:4]  XOR mask[0:4] Ack    = chunk[4:8]  XOR mask[4:8] TSval  = chunk[8:12] XOR mask[8:12] mask   = HMAC-SHA256(hmacKey, "zpl4-mask"  || seqNum) IPv4ID = HMAC-SHA256(hmacKey, "zpl4-check" || seqNum || chunk)[12:14] hmacKey = HMAC-SHA256(secret, "CITP-v3-Phantom-Entanglement-Key") Приёмник знает `seqNum` (синхронный счётчик), восстанавливает `chunk` XOR-ом и проверяет подлинность через `IPv4ID` за O(1). себя (тег считался от неизвестного приёмнику чанка). Здесь декодирование честное: данные восстанавливаются только из проводных полей. целостность, энтропия заголовков ≥ 7.9 бит/байт). Отправка сырых TCP-заголовков на пр
---
Документ описывает план реализации четырёх направлений, предложенных владельцем проекта: 1. **Censor-as-a-Relay** — сама функция вердикта ТСПУ (block-страница / RST) как модуляция данных. 2. **BGP-транспорт** — модуляция глобального состояния маршрутизации, читаемая через публичные сервисы. 3. **Мультиканальная доска объявлений** — комбинация BGP + DNS-зона + CDN-кэш + CT-логи. 4. **ТСПУ как общий оракул** — детерминированные правила цензора как синхронизированный генератор расписания и кодовой книги. Принцип: всё это — **только control-plane** (команды ≤1 КиБ: next-entry, ключи, координаты). Пользовательский трафик идёт через существующий data-plane (mux/TLS), он не меняется. Каждый этап заканчивается тестами и живой проверкой на сети владельца. Записи о работе — в `agent.md`. Цель: превратить лабораторный CAR в канал, пригодный для реального цензора. Критерий готовности: 8/8 бит сообще
---
Инструмент: `cmd/chaos-metrics` (сборка и прогон на RU-ноде, Go 1.26.3). Лог прогона: `/root/chaos-metrics.log`. Методическая честность: Sanity машинерии (связь ε=0, сайты независимы): 8/8 положительных экспонент, h_KS=6.32 бит/итер (λ1=0.835 при μ≈3.9–3.97) — измеритель верен. Прод-поля (как выбрал DeriveField с пробой сходимости наблюдателя), 9 эпох (1, 2, 3, 42, 77, 78, 1000, 424242, 999999): Развёртка по силе связи (μ и топология эпохи 424242, eps1=eps2=ε): Механизм (главный структурный вывод): DeriveField выбирает поля под СХОДИМОСТЬ НАБЛЮДАТЕЛЯ (условные λ<0 — `convergesAt`, schedule.go). Сильная диффузионная связь, обеспечивающая сходимость, топит и автономные экспоненты: автономная динамика прод-решётки квазипериодична (одна ~нулевая экспонента, остальные отрицательные). Хаос живёт при ε ≲ 0.05, сходимость наблюдателя требует ε ≳ 0.35 — режимы несовместны на одном поле. Следствия
---
Built with Go 1.26.3 from the current `/files/VPN` source after the Priority-0 fixes. Copies are also installed as `bin/cham-server-linux` and `bin/cham-server.exe`. Verify after copying: sha256sum cham-server-linux-amd64 Get-FileHash .\cham-server-windows-amd64.exe -Algorithm SHA256 sudo install -d -m 0750 /etc/chameleon sudo install -m 0755 cham-server-linux-amd64 /usr/local/bin/cham-server Generate the node key only for a new node. Do not overwrite an existing key: sudo /usr/local/bin/cham-server -genkey -keyfile /etc/chameleon/server.key sudo chmod 0600 /etc/chameleon/server.key Put one approved client public key on each line: sudoedit /etc/chameleon/clients.txt sudo chmod 0600 /etc/chameleon/clients.txt Create `/etc/systemd/system/cham-server.service`: [Unit] Description=Chameleon CITP Node After=network-online.target Wants=network-online.target [Service] Type=simple ExecStart=/usr/
---
R1 raw artifacts: ../research/ks-r1-20260908/README.md and its SHA256.json. Prior RU runs measured 24016/24016 length/roundtrip cases and matching Go/Python hashes over3670016 state integers. Seal +28 is nonce12+AEADtag16, NOT outer IP overhead. Constant overhead preserves input-length differences under fixed packetization; no DPI classifier was evaluated. Negative conditional sum is insufficient: all32 fixed4 fields had positive top estimates despite negative sums. For alternate4 the top median was -1.413498 bits/iteration on the16384-step window and terminal RMS0 for all32. Exact observations and synthetic fields are not production keys or a lossy modem channel. Smooth finite-time Lyapunov estimates along Q16.48 orbits do not establish finite-state asymptotic entropy or crypto security. KS uses a local key schedule, not the receiver observer. Synchronization improvements are not automa
---
Метод получения новых проверяемых результатов в проекте Chameleon CITP / chaossync / KS. Ядро: сначала развернуть пространство задачи целиком, затем свернуть его в одно измеримое утверждение и попытаться его убить замером. 1. Прочитать `VPN/agent.md` — append-only журнал с обязательными правилами для агента. 2. Прочитать `VPN/README.md` §7 «Матрица зрелости» — что зелёное, а что только заявлено. 3. Прочитать доменный документ: `docs/CHAOSSYNC.md`, `docs/CHAOS-METRICS.md`, `docs/KS.md`, 4. Не повторять работу из раздела «Completed and verified». СТОП-правило: никогда не читать, не печатать и не публиковать приватные ключи (`bin/data/*.key`, `/root/build/*.key`), пароли, bearer-токены и деплой-креды. Структурные выводы обязаны быть ключ-независимыми: мерить на `SelftestMaster`. Правило пересечений: открытия живут не на осях, а в их пересечениях. Пример: Э-A обнаружил, что хаос требует eps 
---
Перед любым числом, которое попадёт в журнал или вывод. 1. Сначала провалидировать измеритель на известном ответе (sanity). 2. Зафиксировать условия: seed, версия, выборка, окно, команда. 3. Прогнать замер, сохранить сырой вывод. 4. Сравнить с нулевой гипотезой и с ожидаемым механизмом. 5. Записать что результат НЕ доказывает.
---
Kazhdyy front - ubivaemoe utverzhdenie. Esli zamer dast drugoe, utverzhdenie umiraet. Cel: vybrat temu, kotoruyu mozhno ubit odnim chestnym zamerom (L1-L4). Utverzhdenie: net svyazki parametrov, pri kotoroy odnovremenno h_KS > 0 i nablyudatel Pecora-Carroll skhoditsya. Izmereno: khaos trebuet eps <= 0.05, skhodimost eps >= 0.35; oblast mezhdu nimi nikto ne kartografiroval dlya Q16.48-reshetki s dvumya eps. Kak ubit: skan po (eps1, eps2), iskat okno gde lambda1 > 0 I BER kontrolya = 0. Utverzhdenie: MI-knee ~2.5-2.6 bit/sempl - fizicheskiy potolok emkosti kanala, a ne artefakt vyborki. Izmereno: M=8 daet 2.46 pri dmax=2^-8; CSK 0.031 (x83 proigrysh). Kak ubit: postroit MI(M) pri rastushchem M i oknakh; esli knee sdvigaetsya - potolka net. Utverzhdenie: pri L >= 12 upravlenie Hayes-Grebogi-Ott dopuskaet konveyernuyu podachu bez poteri BER=0. Izmereno: L>=12 -> 100% kontrol, BER 0.00; bez k
