# Publication status / Статус публикации

This public repository is currently a documentation-only placeholder. The cleaned source tree is staged separately in the connected project filesystem, but the GitHub upload tool accepts file contents rather than importing that mounted directory. Do not build or use this repository until the source tree is uploaded and checks pass.

Этот публичный репозиторий пока содержит только документацию. Очищенные исходники подготовлены отдельно в подключённой файловой системе проекта, однако GitHub-загрузчик принимает содержимое файлов и не импортирует смонтированный каталог. Не собирайте и не используйте этот репозиторий, пока исходники не будут загружены и проверены.